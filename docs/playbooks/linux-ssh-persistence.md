# Linux SSH persistence triage

Validate the source and account, reconstruct authentication timing, inspect sudo logs and cron content, collect process/network evidence and identify affected credentials. Escalate an unexplained chain and coordinate containment with the system owner.
